version: 2

builds:
  - id: rmgedcomx
    main: ./cmd/server
    binary: rmgedcomx
    goos:
      - windows
      - darwin
    goarch:
      - amd64
      - arm64

archives:
  - formats: [zip]

files:
  - royal92.rmtree
  - gedcomx_browser.py

release:
  github:
    owner: aviast
    name: rmgedcomx
